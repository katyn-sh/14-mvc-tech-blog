<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Talk</title>

  <!-- Core theme CSS (includes Bootstrap)-->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
</head>

<body>
  <div class="wrapper">
    <!-- header-->
    <header class="p-3 text-bg-dark">
      <div class="container">
        <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
          <h1 href="/" class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
            The Tech Talk
          </h1>

          <div class="text-end">
            <nav>
              <a href="/" class="btn btn-warning">Home</a>
              {{#if loggedIn}}
              <a href="/dashboard" class="btn btn-warning"> Dashboard</a>
                <button id="logout" class="btn btn-warning">Logout</button>
                {{else}}
                <a href="/login" class="btn btn-warning">Login</a>
                {{/if}}
            </nav>
          </div>
        </div>
      </div>
    </header>

    <main>
      <!-- Blog entries-->
      <div class="col-lg-12">
        {{{ body }}}
      </div>

    </main>

    <!-- Footer-->
    <div class="container">
      <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
        <div class="col-md-4 d-flex align-items-center">
          <a href="/" class="mb-3 me-2 mb-md-0 text-muted text-decoration-none lh-1">
            <svg class="bi" width="30" height="24">
              <use xlink:href="#bootstrap" />
            </svg>
          </a>
          <span class="mb-3 mb-md-0 text-muted">&copy; 2023 | Andrew Huynh</span>
        </div>

        <ul class="nav col-md-4 justify-content-end list-unstyled d-flex">
          <li class="ms-3"><a class="text-muted" href="https://github.com/katyn-sh">GitHub</a></li>
        </ul>
      </footer>
    </div>
  </div>

</body>

</html>

{{#if loggedIn}}
<script src="/js/logout.js"></script>
{{/if}}